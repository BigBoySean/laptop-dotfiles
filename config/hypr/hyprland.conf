# Personal entrypoint — sources upstream Caelestia, then my overrides.
# (Previous Hyprland-generated stub saved as hyprland.conf.stub.bak)
source = ~/.config/hypr/hypr/hyprland.conf
# Personal binds and overrides live in ~/.config/caelestia/hypr-user.conf
# (that file is sourced last by the Caelestia config above, so it wins).
